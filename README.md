# Base64
  A Go implementation of the Base64 encoding algorithm that converts text to its Base64 representation by processing binary data in 6-bit chunks.
  Code shows the actual algorithm step by step with binary output.

```We just complete with encoding, Next step is making decoding```
